[project]
name = "task-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sangeeth", email = "geeth.c.j@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
task-manager = "task_manager:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
